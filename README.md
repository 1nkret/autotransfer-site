# 🚛 AutoLogistics – Автомобільні перевезення

Цей проєкт є практичною роботою з веб-програмування в **Одеському національному політехнічному університеті**.  
Мета – реалізувати простий веб-сервіс для подачі онлайн-заявок на перевезення та перегляду автопарку компанії.

## 📌 Функціонал
- Відображення автопарку (вантажні авто + автовози) з бекенду.
- Форма для подачі заявки на перевезення (POST → бекенд).
- Збереження заявок у `orders.json`.
- Відповідь з унікальним ID заявки.
- Простий фронтенд з формами та таблицями.

## 🛠️ Технології
- **Backend:** [FastAPI](https://fastapi.tiangolo.com/), Python 3.11+
- **Frontend:** HTML + CSS + JavaScript (fetch API)
- **Data storage:** JSON-файли (`autopark.json`, `orders.json`)

## 📂 Структура
```
project/
│ main.py
│ README.md
├── backend/
│   └── routers/
│       └── orders.py
├── front/
│   ├── static/
│   │   ├── css/
│   │   │   └── styles.css
│   │   └── js/
│   │       └── app.js
│   └── templates/
│       └── index.html
├── autopark.json
└── orders.json
```

## ▶️ Запуск
1. Встановити залежності:
   ```bash
   pip install fastapi uvicorn
   ```
2. Запустити сервер:
   ```bash
   uvicorn main:app --reload
   ```
3. Відкрити в браузері: [http://127.0.0.1:8000](http://127.0.0.1:8000)

## 📑 Приклади API
- `GET /api/fleet` → список машин
- `POST /api/applications` → створення заявки  
  **Body:**
  ```json
  {
    "from": "Одеса",
    "to": "Львів",
    "weight": 1200,
    "cargoType": "standard",
    "urgency": "normal",
    "distance": 500,
    "phone": "+380931234567",
    "email": "example@gmail.com"
  }
  ```
  **Response:**
  ```json
  { "id": "a1b2c3d4", "status": "ok" }
  ```

---

✍️ **Примітка:** Цей код розроблений як **практична робота в Одеському національному політехнічному університеті** для навчальних цілей.
